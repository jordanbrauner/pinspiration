.board
  %h3= link_to board.title, board
  %ul.options
    %li= link_to 'edit', edit_board_path(board)
    %li= link_to 'destroy', board_path(board), method: :delete, data: { confirm: 'Are you sure?' }
